{
  "name": "nestjs-converter",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@nestjs/common": "^8.0.4",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.2.0"
  },
  "devDependencies": {
    "@types/node": "^16.4.2",
    "prettier-eslint": "^12.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
